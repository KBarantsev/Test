boxes_distribution_train = [len(data_train_pd['ann'][i]['bboxes']) for i in range(len(data_train_pd))]
boxes_distribution_val = [len(data_val_pd['ann'][i]['bboxes']) for i in range(len(data_val_pd))]

ser_train = pd.Series(boxes_distribution_train)
ser_val = pd.Series(boxes_distribution_val)

ser_train.describe()

ser_val.describe()

ser_train.hist(figsize=(10,5),rwidth=0.5,grid=False,bins=50)
plt.xticks(rotation=90)
plt.title('Распределение по колличеству боксов train')
plt.show()


ser_val.hist(figsize=(10,5),rwidth=0.5,grid=False,bins=50)
plt.xticks(rotation=90)
plt.title('Распределение по колличеству боксов val')
plt.show()
