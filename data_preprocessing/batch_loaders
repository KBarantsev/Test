train_dataset = MyDataset(data_train_pd, mode='train')
val_dataset = MyDataset(data_val_pd, mode='val')
test_dataset = MyDataset(data_test_pd, mode='test')


#выведем случайную картинку

image = train_dataset[8][0]

import matplotlib.pyplot as plt
plt.figure(figsize=(20, 12))
plt.imshow(image.permute(1, 2, 0))


#создание загрузчиков батчей
from torch.utils.data import Dataset, DataLoader
batch_size = 7
train_loader = DataLoader(train_dataset, batch_size=batch_size, shuffle=True,num_workers=2)
val_loader = DataLoader(val_dataset, batch_size=batch_size, shuffle=True,num_workers=2)
#test_loader = DataLoader(test_dataset, batch_size=batch_size, shuffle=True,num_workers=2)
